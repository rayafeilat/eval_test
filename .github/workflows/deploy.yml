name: evaluation pipeline 
on:
  push:
    branches:
      - main
jobs:
  update application:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: update on vm
        run: |
          docker compose up --build -d 
